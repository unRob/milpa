summary: prints a variable from the environment
description: |
  what the summary says
arguments:
  - name: var
    description: the variable to print
    required: true
options:
  match:
    description: if specified, will use `VAR` as a pattern to filter all environment variables
    type: bool
