summary: Creates a new tag and updates the changelog
description: |
  Longer description of how it does the thing
# see `milpa help docs command spec` for all the options
arguments:
  - name: increment
    description: the semver increment to apply to the last git version
    values: [major, minor, patch]
options:
  pre:
    values: [alpha, beta, rc]
    description: create a pre-release
