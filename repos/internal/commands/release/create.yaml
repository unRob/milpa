summary: Creates a new tag and updates the changelog
description: |
  Figures out the next version by looking at pending changelog notes, then prompts to create a new tag. Automation might trigger a release if github is in a good mood.
options:
  pre:
    values:
      static: [alpha, beta, rc]
    description: create a pre-release
