summary: show changelog entries for a revision
description: |
   If no `VERSION` is provided, or if `VERSION` matches the pre-release semver spec, the latest commit will be used as the VERSION
arguments:
  - name: version
    description: the version to generate release notes for
    default: HEAD
options:
  header-offset:
    description: Increase header levels by this offset
